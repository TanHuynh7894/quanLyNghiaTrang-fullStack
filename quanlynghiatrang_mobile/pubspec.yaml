name: quanlynghiatrang_mobile
description: "Quan Ly Nghia Trang mobile app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  maplibre_gl: ^0.24.1

  # THU ÂM = flutter_sound (thay cho record)
  flutter_sound: ^9.2.13
  path_provider: ^2.1.4
  permission_handler: ^11.3.0

  flutter_dotenv: ^5.1.0
  url_launcher: ^6.3.0
  geolocator: ^12.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
