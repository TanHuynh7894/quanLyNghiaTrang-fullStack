version: "3.9"

services:
  whisper-asr:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: whisper-gpu-server
    container_name: whisper-asr

    ports:
      - "8010:8000"

    environment:
      DEFAULT_MODEL: "kiendt/PhoWhisper-large-ct2"
      CT2_DEVICE: "cuda"
      CT2_COMPUTE_TYPE: "float16"
      HF_HOME: "/root/.cache/huggingface"
      # Dùng host.docker.internal để gọi BE trên máy host (Windows/macOS)
      BE_BASE_URL: "http://host.docker.internal:5000"
      LOG_LEVEL: "INFO"
      TAGGER_BACKEND: "none"      # "seq2seq" | "gec_tagger" | "none"
      TAGGER_MODEL: ""
      TAGGER_DEVICE: "cuda"

    volumes:
      - "E:/D/vnpt/quanLyNghiaTrang_1/whisper/.cache/huggingface:/root/.cache/huggingface"
      - "E:/D/vnpt/quanLyNghiaTrang_1/whisper/server/app.py:/app/app.py:ro"
      # - "E:/D/vnpt/quanLyNghiaTrang_1/whisper/models/tagger:/models/tagger:ro"  # nếu dùng tagging local

    # Cách đúng cho GPU trong Docker Compose hiện đại:
    gpus: "all"

    restart: unless-stopped

    command:
      [
        "uvicorn",
        "app:app",
        "--host", "0.0.0.0",
        "--port", "8000",
        "--timeout-keep-alive", "120",
        "--log-level", "info",
        "--reload"
      ]

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 5
